# 阶段 F 冒烟验证记录（2026-03-09）

## 1. 验证范围

本次验证基于阶段 F 已完成实现，覆盖以下模块：

- 主入口 [`JJH_download.pyw`](JJH_download.pyw)
- 下载管理器 [`core/download_manager.py`](core/download_manager.py)
- 历史仓储 [`core/history_repo.py`](core/history_repo.py)
- yt-dlp 命令构建 [`core/ytdlp_builder.py`](core/ytdlp_builder.py)
- 顶部工具栏 [`ui/app_shell.py`](ui/app_shell.py)
- 运行状态窗口 [`ui/history_actions.py`](ui/history_actions.py)
- 下载页容器 [`ui/download_tab.py`](ui/download_tab.py)
- 单视频页 [`ui/pages/single_video.py`](ui/pages/single_video.py)
- 批量页 [`ui/pages/batch_source.py`](ui/pages/batch_source.py)

## 2. 已完成验证项

### 2.1 全量语法校验

已执行以下命令：

```bash
python -m py_compile JJH_download.pyw core\youtube_models.py core\ytdlp_builder.py core\history_repo.py core\download_manager.py ui\app_shell.py ui\history_actions.py ui\app_actions.py ui\download_tab.py ui\video_actions.py ui\pages\single_video.py ui\pages\batch_source.py
```

结果：通过，退出码 0。

### 2.2 阶段 F 功能点静态核对

已核对如下能力已经落地：

- 归档目录字段与归档路径解析已接入批量任务与下载执行链路
- 历史数据库已支持归档上下文字段与迁移补列
- 批量页已具备最近一次处理结果与错误摘要展示
- 顶部工具栏已提供“🩺 运行状态”入口
- 运行状态窗口已展示数据库状态、任务计数与最近运行问题
- 运行状态摘要已能同步显示在顶部工具栏
- 批量解析、批量入队、yt-dlp 更新均已加入防重入保护
- 关闭窗口时已对运行中任务、等待中任务、批量处理、yt-dlp 更新进行提醒

### 2.3 阶段 C / D / E 主流程兼容性静态检查

已确认以下关键主流程接口仍保持可调用状态：

- 单视频任务构建入口 [`prepare_standard_task()`](ui/input_validators.py:174)
- 单视频直接下载入口 [`prepare_direct_task()`](ui/input_validators.py:196)
- 批量解析入口 [`fetch_batch_entries()`](ui/pages/batch_source.py:425)
- 下载执行主链路 [`_run_ytdlp_task()`](core/download_manager.py:189)
- 认证异常提示链路 [`_notify_auth_issue()`](core/download_manager.py:172)
- 历史窗口入口 [`show_history_window()`](JJH_download.pyw:326)
- 认证状态窗口入口 [`show_auth_status()`](JJH_download.pyw:335)
- 运行状态窗口入口 [`show_runtime_status()`](JJH_download.pyw:340)

## 3. 本轮验证结论

当前已完成的验证结论如下：

- 代码整体语法有效，当前阶段改动未引入编译级错误
- 阶段 F1 / F2 / F3 / F4 的主要落地点已能在代码层面闭环
- 阶段 E 的认证、历史数据库、失败记录、运行问题沉淀能力仍保留
- GUI 主入口、单视频页、批量页、历史入口、认证入口、运行状态入口之间的引用关系完整
- 程序已在真实 GUI 环境中成功启动，单视频页与批量页可以正常进入
- 下载任务管理已调整为独立标签页，用户可在“🧾 下载队列”页看到已加入的任务
- 单视频 / 批量加入队列的主链路已通过真实交互验证，至少确认“任务创建成功并显示在队列中”这一关键前置环节正常

## 4. 已完成的真实 GUI 验证补充

本轮在静态冒烟之外，已补充完成以下真实 GUI 验证：

- 主程序可以正常启动，无启动即崩溃问题
- 单视频页可以正常进入
- 批量页可以正常进入
- 新增的“🧾 下载队列”标签页可以正常显示
- 用户已实际将任务加入队列，并确认可在“🧾 下载队列”页看到对应任务

本轮真实验证过程中还定位并修复了两类关键问题：

- [`ui/download_tab.py`](ui/download_tab.py) 在移除旧内嵌队列后残留未定义变量引用，导致启动时报 `task_tree` 未定义错误；现已清理为由独立队列页负责绑定队列控件
- [`ui/input_validators.py`](ui/input_validators.py) 中任务构造未传入 `save_path`，导致创建 [`YouTubeTaskRecord`](core/youtube_models.py:61) 时抛出缺参异常；现已完成适配修复

## 5. 尚未完成的真实链路验证

以下项目仍需在真实 GUI 环境中继续补充验证：

- 单视频页实际获取格式、选择格式、开始下载并成功落盘是否正常
- 批量页实际解析播放列表 / 频道并显示条目是否正常
- 批量条目加入队列后开始下载是否正常
- 历史去重是否能正确跳过已成功记录
- 失败场景是否能同步出现在结果卡片、日志、顶部运行状态、运行状态窗口
- 归档目录是否按“频道 / 来源 / 日期”正确生成
- 关闭窗口确认提示在真实后台任务执行中是否符合预期

## 6. 当前判定

本文件现可作为阶段 F5 的第二轮验证记录，说明：

- “静态冒烟验证”已完成
- “真实 GUI 可启动 + 可入队 + 可见队列”验证已完成
- “真实下载成功 / 失败 / 去重 / 归档落盘”验证仍需继续补充

后续正式结项时，应将真实下载样本、失败样本、去重样本与归档落盘样本统一收口到 [`plans/阶段F结项与冒烟记录.md`](plans/阶段F结项与冒烟记录.md)
