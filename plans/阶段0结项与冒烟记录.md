# 阶段0结项与冒烟记录

## 1. 阶段信息

- 阶段名称：阶段0
- 阶段主题：基线稳固与可回归验证
- 执行日期：2026-03-14
- 执行人：Codex
- 对应执行清单：[plans/阶段0执行清单.md](./阶段0执行清单.md)

---

## 2. 本阶段目标回顾

阶段0的目标不是新增用户可见大功能，而是确认当前程序具备继续开发的最低条件：

- 已有最小自动化测试骨架
- `ytdlp_builder` 与 `history_repo` 已有基础测试
- 下载前结构化命令摘要可落日志
- 失败阶段能够分类写入历史
- 已能执行一轮真实链路冒烟

本次实际执行结果：

- 最小测试骨架：已具备
- 自动化测试：已执行并通过
- 结构化下载摘要：已在真实下载链路中验证
- 失败阶段分类：已在认证失败样本中验证
- 真实链路冒烟：已执行

结论：阶段0目标已达到，可作为进入阶段1的基线。

---

## 3. 自动化验证记录

### 3.1 执行命令

```bash
python -m pytest tests -q
```

### 3.2 结果

- 结果：通过
- 用例数：18
- 总结：`18 passed in 0.49s`

### 3.3 已覆盖内容

- `tests/test_ytdlp_builder.py`
  - 输出模板
  - 自定义文件名
  - 音频模式参数
  - 视频模式参数
  - 元数据与封面参数
  - 限速与节流参数
  - cookies / PO Token 参数注入
- `tests/test_history_repo.py`
  - sqlite 初始化
  - 成功记录写入
  - 失败记录写入
  - 旧字段兼容迁移
  - `video_id` / `playlist_id` 提取
- `tests/test_input_validators.py`
  - 整数输入回退
  - 自定义文件名校验
  - 格式选择提取
  - 输出格式联动
  - 直接任务构建
  - YouTube URL 校验

### 3.4 未覆盖内容

- 真实网络下载链路
- 真正的 cookies 受限视频场景
- GUI 交互层
- 批量页面的 Tk 组件交互

这些部分已通过下面的手工冒烟补足核心路径。

---

## 4. GUI 启动冒烟

### 4.1 执行命令

```bash
python -c "import subprocess,time,sys; p=subprocess.Popen([sys.executable,'YCB.pyw']); time.sleep(3); print(f'alive={p.poll() is None}; returncode={p.poll()}'); p.terminate() if p.poll() is None else None"
```

### 4.2 结果

- 输出：`alive=True; returncode=None`
- 结论：程序可以正常启动，阶段0相关改动未导致 GUI 启动失败

---

## 5. 真实链路冒烟记录

本轮冒烟主要使用以下样本：

- 成功下载样本：`https://www.youtube.com/watch?v=dQw4w9WgXcQ`
- 认证/受限提示样本：`https://www.youtube.com/watch?v=BaW_jenozKc`
- 批量解析样本：`https://www.youtube.com/@YouTube/videos`

所有临时输出均位于：

- `temp/stage0_smoke_run/`
- `temp/stage0_log_probe/`

### 5.1 单视频最佳兼容下载

- 样本链接：`https://www.youtube.com/watch?v=dQw4w9WgXcQ`
- 配置：`best_compat`，输出 `mp4`
- 结果：通过
- 实际状态：`完成`
- 输出文件：
  - `temp/stage0_smoke_run/best_compat/Rick Astley - Never Gonna Give You Up (Official Video) (4K Remaster).mp4`
- 历史写入结果：成功，历史状态为 `完成`

### 5.2 单视频音频下载

- 样本链接：`https://www.youtube.com/watch?v=dQw4w9WgXcQ`
- 配置：`audio_only`，输出 `mp3`
- 结果：通过
- 实际状态：`完成`
- 输出文件：
  - `temp/stage0_smoke_run/audio_only/Rick Astley - Never Gonna Give You Up (Official Video) (4K Remaster).mp3`
- 历史写入结果：成功，历史状态为 `完成`

### 5.3 自定义文件名下载

- 样本链接：`https://www.youtube.com/watch?v=dQw4w9WgXcQ`
- 配置：自定义文件名 `stage0_custom_name`
- 结果：通过
- 实际状态：`完成`
- 输出文件：
  - `temp/stage0_smoke_run/custom_filename/stage0_custom_name.mp4`
- 历史写入结果：成功，历史状态为 `完成`

### 5.4 批量解析

- 样本链接：`https://www.youtube.com/@YouTube/videos`
- 调用链路：`YouTubeMetadataService.fetch_channel_entries()`
- 结果：通过
- 返回状态：`ok=True`
- 解析条目数：`100`
- 来源类型：`channel`
- 首条标题：`@SANTOS BRAVOS watched WHAT?! | Watch History`

### 5.5 队列执行冒烟

- 验证方式：使用两个音频任务进入下载队列，串行执行
- 输入链接：`https://www.youtube.com/watch?v=dQw4w9WgXcQ`
- 自定义输出：
  - `batch_queue_1.mp3`
  - `batch_queue_2.mp3`
- 结果：通过
- 任务1状态：`完成`
- 任务2状态：`完成`
- 实际输出文件：
  - `temp/stage0_smoke_run/queue/batch_queue_1.mp3`
  - `temp/stage0_smoke_run/queue/batch_queue_2.mp3`
- 历史记录数：`2`

说明：
本项验证的是“入队 -> 调度 -> 下载 -> 历史写入”主链路。批量页面 GUI 选择与入队交互本身不在阶段0验证范围内。

### 5.6 认证/受限提示链路

- 样本链接：`https://www.youtube.com/watch?v=BaW_jenozKc`
- 配置：`best_compat`
- 结果：已触发受限提示链路
- 实际状态：`失败`
- 失败摘要：`检测到访问受限、地区限制或资源不可用问题`
- 历史顶部记录状态：`失败`
- 历史顶部失败阶段：`auth`
- 运行时提示内容：
  - `检测到访问受限、地区限制或资源不可用问题`
  - 行动建议：优先检查 `cookies`、视频可见性、地区限制与代理设置

说明：
本样本没有验证出“cookies 文件过期”这一种特定文案，而是验证出更上层的认证/访问受限诊断链路已经能落到提示和历史分阶段记录。

### 5.7 历史写入验证

成功路径写入情况：

- 最佳兼容下载：`1` 条成功记录
- 音频下载：`1` 条成功记录
- 自定义文件名下载：`1` 条成功记录
- 队列任务：`2` 条成功记录

失败路径写入情况：

- 认证/受限样本：已写入失败记录
- 顶部失败阶段：`auth`

结论：
阶段0要求的“成功与失败历史都能落盘”已实际验证。

---

## 6. 结构化日志与失败分类验收

### 6.1 下载前结构化摘要

已通过真实下载链路验证，日志中可看到以下摘要项：

- `准备执行下载命令`
- `task_id`
- `preset`
- `format`
- `url`
- `output`
- `cookies`
- `po_token`
- `merge`
- `cmd_preview`

实际验证位置：

- `temp/stage0_log_probe/`

实际验证结论：

- `summary_present=True`

### 6.2 失败阶段分类

已在认证/受限样本中验证：

- 失败状态：`失败`
- 历史失败阶段：`auth`
- 失败摘要：`检测到访问受限、地区限制或资源不可用问题`

结论：
阶段0要求的失败阶段分类已落入真实失败样本。

---

## 7. 兼容性影响

本轮实际冒烟结果表明，以下主链路可用：

- 单视频最佳兼容下载：可用
- 单视频音频下载：可用
- 自定义文件名下载：可用
- 队列下载：可用
- 批量解析：可用
- 历史记录写入：可用
- GUI 启动：可用

本轮没有发现“阶段0导致主下载链路整体不可用”的问题。

---

## 8. 已知问题与风险

### 8.1 标题获取阶段存在日志噪音

在 `temp/stage0_log_probe/` 的真实下载链路中观察到：

- 标题最终获取成功
- 但日志中先出现了“标题/元数据获取失败，原因暂未识别”的警告

这说明标题获取诊断链路可能存在噪音日志问题，容易让开发人员误判为真实失败。

影响：

- 不阻塞阶段1准入
- 但建议在后续阶段尽早收敛这类误报

### 8.2 认证诊断样本覆盖的是“受限/Forbidden”而非“cookies 过期”特定文案

本轮已验证：

- 认证提示链路能触发
- 历史失败阶段能写入 `auth`

本轮未单独验证：

- “cookies 文件失效/过期”的专门文案

影响：

- 不阻塞阶段1
- 建议后续若拿到更稳定的登录受限样本，再补一轮更细的认证冒烟

---

## 9. 阶段结论

- 结论：允许进入阶段1
- 判断依据：
  - 自动化测试已通过
  - GUI 启动冒烟已通过
  - 单视频下载主链路已通过
  - 队列下载已通过
  - 批量解析已通过
  - 历史成功/失败写入已通过
  - 结构化摘要与失败阶段分类已通过真实样本验证

---

## 10. 附件与补充说明

- 自动化测试命令：
  - `python -m pytest tests -q`
- GUI 启动冒烟命令：
  - `python -c "import subprocess,time,sys; p=subprocess.Popen([sys.executable,'YCB.pyw']); time.sleep(3); print(f'alive={p.poll() is None}; returncode={p.poll()}'); p.terminate() if p.poll() is None else None"`
- 真实链路输出目录：
  - `temp/stage0_smoke_run/`
  - `temp/stage0_log_probe/`

